replicaCount: 1

image:
  #repository: mi-micro
  repository: ghcr.io/alexanderpgarnica/microkube
  tag: "1.0"
  #pullPolicy: IfNotPresent
  pullPolicy: Always

service:
  type: NodePort
  port: 80
  targetPort: 8080
  nodePort: 30080

env:
  SERVICE_NAME: "mi-micro"
  ENV: "k8s-local"